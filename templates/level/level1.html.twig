<h1>Configuration</h1>
<p>
    To connect to the database, use <a href="https://mysql.hacktime.de">PhpMyAdmin</a> with your user data. <br/>
    On the other hand you can also connect with a tool of your choice! <br/>
<dl>
    <dt>Host</dt>
    <dd>hacktime.de</dd>
    <dt>Port</dt>
    <dd>2705</dd>
    {% if user is defined %}
        <dt>User</dt>
        <dd>{{ user }}</dd>
    {% endif %}
    {% if password is defined %}
        <dt>Password</dt>
        <dd>{{ password }}</dd>
    {% endif %}
</dl>
</p>
<p>
    Open the database and take a look at the existing tables.
</p>

<h2>Insert Command</h2>
<p>
    A SQL INSERT statement adds one or more records to any single table in a relational database.
</p>